#include<stdio.h>
int main()
{
   char c;

   printf ("Enter a character n");
   scanf ("%c", &c);

   if (c>64 && c<91)
   {
         printf ("It is uppercase character");   
   }     
    else
   {
         printf ("It is in lowercase character");
   }

   return 0;
}
